why react
componetn based
spa
virtual dom

props
props are read only inputs
used to pass information from parent to child
1) normal props
<Doctor name="teja"/>

2) child prop
<Doctor>Teja</Doctor>

state
state is like a variable it is used to manage
dynamic nature of a component
component rerenders whenever the state changes

how to define state
import {useState} from 'react'

const [state,setState]=useState(initialvalu)

state - which holds initial value
setState- it is a function which changes the state

const [count,setCount]=useState(0)

<button onClick={()=>setCount(count+1)}>inc</button>



useEffect 
it is hook that is used to manage side effects (data fetching from api,
 localstorage, settimeout setinterval)

import {useEffect} from 'react'

1st - without any empty or dependenccy array
it will run whenever the state changes
useEffect(()=>{
    console.log('running')
    fetchdata()
})

2nd- useeffect with empty array or empty dependency
it will run only one time
useEffect(()=>{
     console.log('running')
},[])

3rd - useeffect with dependency
it will run only when there is a change in dependency
useEffect(()=>{
     console.log('running')
},[apidata])


map 
it is used for rendering a list
map() is a JavaScript array method used to iterate over arrays and
return UI elements dynamically.

key
A key is a unique identifier used by React to efficiently update list items.


conditional rendering

{condtion? (<comp1>): (<comp2>)}
if condition is true comp1 is executed else comp2


filter
it is used to apply boolean function for every element and returns
filtered array


react router dom
it is used for navigation or routing in react because in react we have spa so we can't use 
anchor tag for spa 

npm install react-router-dom


advanced topics
1) protected routes
2) useMemo
it is hook that is used to optimize the performance of an react 
and it will memorize the expensive calculations and it will rerender only
whenever dependencies changes
3) custom hooks
4) error handling methods


props drilling
   A      B      C
 data    data    usecase of data


to overcome----global state management
useContext
redux
redux toolkit
